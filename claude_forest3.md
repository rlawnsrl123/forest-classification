# 산림 분류 — 지역 간 전이 탄소 오차 분석 (작업 기록 3)

`PROJECT.md`가 프로젝트 전체 상태, 이 파일이 **현재 진행 중인 분석**입니다.
`HANDOFF.md`를 대체합니다 (light 결과와 2요인 분해가 반영됨).

노트북: `03_experiments.ipynb` (Colab 무료 티어)

---

## 0. 현재 상태

- 셀 [6] `light` 9조합 **완료**
- 셀 [6] `full` 9조합 **실행 중**
- 셀 [7]~[10] 대기 (코드는 §7에 있음)
- 2요인 구조 발견 → 검산 완료, 교정 사항 §6

---

## 1. 노트북 구조

주석 번호와 셀 인덱스가 다름. **주석 번호로 통일해서 부름.**

| 주석 | 인덱스 | 내용 | 상태 |
|---|---|---|---|
| [1] | 0 | 셋업, `make_feats`, `extract_samples` | 완료 |
| [2] | 1 | `S` = 세 지역 샘플 로드 | 완료 |
| [3] | 2 | 대전 검증 | 완료 |
| [4] | 3 | 전이 실험 12조합 (무거운 설정) | 완료 → PROJECT.md 5-4 |
| [5] | 4 | 탄소 오차 6조합 (경량) | 완료, **재실행 금지** |
| [6] | 5 | 예측 저장 9조합 × 2설정 | light 완료 / full 진행 |
| [7]~[10] | — | 분석 | 대기 |

**셀 [5] 재실행 금지 이유**: `rng_g`가 모듈 레벨 난수라 셀 실행 순서에 의존.
다시 돌리면 [4] 결과를 재현 못 함. 셀 [6]은 조합별 독립 시드로 이 문제를 제거.

---

## 2. 데이터 구조

### 샘플 npz — `{OUT}/{name}_samples.npz`
키는 **`X, y, tile, sp`** 네 개.

| 키 | 내용 |
|---|---|
| `X` | (n, 38) float32 피처 |
| `y` | 1 침엽 / 2 활엽 / 3 혼효 |
| `tile` | `(row//500)*10000 + (col//500)` — 5km 격자 |
| `sp` | `KOFTR_GROU` 수종코드. **낙엽송 = 13** |

raw row/col은 저장 안 됨. `tile`에서 역산 가능
(`row//500 = tile//10000`, `col//500 = tile%10000`).
→ 타일을 **크게 묶는 건 가능**(10km, 20km), 잘게는 불가.

### 예측 npz — `{OUT}/preds/{a3}_{b3}_{cfg}.npz`
- `a3`/`b3` : `dae`, `hon`, `sun`
- `cfg` : `light`(트리 80, leaf 20, sqrt) / `full`(트리 150, leaf 5)
- 키: `y_true`, `y_pred`, `tile`, `sp`, `cfg`

**셀 [7]~[10]은 이 파일만 읽음. `S` 불필요, 재학습 불필요.**

---

## 3. light 결과

### 3-1. 전체 9조합 탄소 오차

| 학습＼평가 | dae | hon | sun | 행 평균 |
|---|---|---|---|---|
| **dae** | −0.81 | +1.92 | −1.34 | −0.08 |
| **hon** | −1.26 | −0.02 | −2.55 | −1.28 |
| **sun** | +2.16 | +4.21 | +0.51 | +2.29 |
| **열 평균** | +0.03 | +2.04 | −1.13 | +0.31 |

macro: sun→hon 0.484 / sun→dae 0.583 / dae→hon 0.569 /
hon→dae 0.581 / dae→sun 0.550 / hon→sun 0.539

### 3-2. 재현성 확인 — 노이즈 기준선 확보

[5]와 [6]은 샘플링 방식이 다른데(모듈 레벨 `rng_g` vs 조합별 독립 시드) 결과가 거의 같음.

| 조합 | [5] macro | [6] macro | Δ | [5] 탄소 | [6] 탄소 | Δ |
|---|---|---|---|---|---|---|
| sun→hon | 0.481 | 0.484 | +0.003 | +4.20 | +4.21 | +0.01 |
| sun→dae | 0.587 | 0.583 | −0.004 | +2.05 | +2.16 | +0.11 |
| dae→hon | 0.565 | 0.569 | +0.004 | +2.01 | +1.92 | −0.09 |
| hon→dae | 0.582 | 0.581 | −0.001 | −1.23 | −1.26 | −0.03 |
| dae→sun | 0.553 | 0.550 | −0.003 | −1.29 | −1.34 | −0.05 |
| hon→sun | 0.544 | 0.539 | −0.005 | −2.50 | −2.55 | −0.05 |

> **샘플링 노이즈 = macro 0.005, 탄소 0.11%p**

이게 "이 차이가 유의한가"의 기준선. **논문에 이 재현 실험을 그대로 넣을 것.**
심사에서 "난수 아니냐"는 질문을 한 줄로 막음.

부수 확인: [4]의 0.449 vs [5]의 0.481 차이는 노이즈의 7배 → **하이퍼파라미터 차이가 맞음.**

### 3-3. 지역 내 ±1%가 세 지역 전부에서 성립

`dae→dae −0.81` / `hon→hon −0.02` / `sun→sun +0.51`.
PROJECT.md 6-6은 대전 하나였는데 이제 세 지역. **대조군이 단단해짐.**
`dae_dae_full −0.74`도 light와 0.07%p 차이라 설정 무관.

---

## 4. 핵심 발견 — 2요인 구조

### 4-1. 가법모델이 성립

행(학습지역) + 열(평가지역)으로 분해하면 잔차가 0.5%p 이내.

```
탄소오차 ≈ 행평균 + 열평균 − 0.31   ( + 지역 내면 추가 이득 )
```

**세 성분의 뜻이 다름:**

- **열효과** = 그 지역이 얼마나 취약한가. `hon +2.04`로 압도적. 낙엽송이 설명.
- **행효과** = 그 모델이 어떤 편향을 갖고 나가는가. `hon −1.28`.
  낙엽송을 본 모델은 어디 가서든 침엽을 과잉 예측 → 과소추정.
- **대각효과** = 자기 지역에서만 얻는 추가 이득.

홍천이 행에서 최저, 열에서 최고인 것이 **같은 원인의 양면**. 낙엽송 가설이 관통.

### 4-2. 잔차 구조

```
        dae     hon     sun
dae   -0.45   +0.27   +0.18
hon   +0.30   -0.47   +0.17
sun   +0.15   +0.19   -0.34
```

대각 3개만 음수, 비대각 6개 전부 양수. 대각 평균 −0.42, 비대각 +0.21.
**대각이 최저 3개일 우연 확률 = 1/C(9,3) = 1.2%.** 우연으로 보기 어려움.

### 4-3. 검증 — 비대각만으로도 성립 (중요)

R²는 근거가 안 됨(9셀 5모수, 자유도 4). 대신 **대각 3개를 빼고 비대각 6셀만 적합**:

```
비대각 잔차: ±0.013     ← 노이즈 0.11의 1/8
```

자유도 1짜리 검정 가능한 제약이 통과함. 구체적으로 두 순환의 합이 같아야 한다는 제약:

```
dae→hon + hon→sun + sun→dae = 1.92 − 2.55 + 2.16 = +1.53
dae→sun + sun→hon + hon→dae = −1.34 + 4.21 − 1.26 = +1.61
차이 0.08  <  노이즈 0.11    ✓
```

**적합도가 아니라 모델의 예측이 맞아떨어진 것.** 논문에서는 R² 대신 이걸 쓸 것.

### 4-4. 대각 효과는 −0.42가 아니라 −0.63

대각을 적합에 포함하면 행·열 효과가 대각 쪽으로 끌려가 효과가 축소됨.
비대각으로만 적합해 대각을 **외삽**하면:

| | 외삽 | 실측 | 지역 내 이득 |
|---|---|---|---|
| dae→dae | −0.09 | −0.81 | −0.72 |
| hon→hon | +0.75 | −0.02 | −0.77 |
| sun→sun | +0.91 | +0.51 | −0.40 |

평균 **−0.63%p**. 1.5배 큼.

---

## 5. 미해결 — sun 행 이상

`sun +2.29` vs `dae −0.08`. **둘 다 낙엽송 0%인데 2.4%p 차이.**
낙엽송 효과(3.3%p)에 육박하는 크기라 무시 못 함.

낙엽송 단일 요인 해석("노출도 0 → 방향성 없음")으로는 설명 안 됨.
6조합만 봤을 때는 안 보였고, 지역 내 3개가 들어오면서 드러남.

### 유력 가설
순천 침엽수는 편백·곰솔·소나무로 **전부 상록이고 분광 분포가 좁음**.
→ "침엽=상록" 규칙을 좁게 학습
→ 규칙에 안 맞는 침엽 픽셀이 전부 활엽으로 떨어짐
→ 활엽 계수(65) > 침엽(55) → **과대추정(+)**

대전은 리기다소나무 등으로 구성이 섞여 규칙이 느슨 → 오차가 양방향 → 상쇄.

### 주의
`class_weight='balanced'`를 쓰므로 **단순 사전확률 편중으로는 설명 안 됨.**
"순천 침엽 비율 47.6%로 높아서"가 아니라 **분광 분포가 좁아서**여야 함.

### 검증 방법 2개
1. **[7]에서**: sun 행 3개 전부에서 `침엽→활엽` 기여가 양수로 크게 나오는가
2. **분광 분산 비교**: 순천 침엽수 수종별 낙엽기 NDVI 분산을 홍천과 비교.
   5-2에서 홍천은 이미 뽑았으므로 순천만 추가하면 됨.
   순천 분산이 확연히 작으면 가설 지지.

---

## 6. 교정 사항 (반드시 반영)

| 항목 | 내용 |
|---|---|
| **R² 0.977** | 쓰지 말 것. 9셀 5모수, 자유도 4. 대신 §4-3의 순환 제약을 쓸 것 |
| **대각 −0.4%p** | 과소평가. 실제 −0.63%p (§4-4) |
| **대각 효과 교락** | 아래 참조. 검증 전까지 "관찰"로만 |
| **행효과 용어** | 산문은 행 *평균*, 코드는 `r−g` *편차*. 다른 값을 가리킴. 용어 고정 필요 |
| **공식 부호** | `행효과 + 열효과 − 0.31`은 평균으로 읽을 때만 성립. 편차면 `+0.31` |

### 대각 효과 교락 — 가장 중요

셀 [6]에서 대각과 비대각의 **평가셋이 다름**:

```python
if a == b:   te = sub2(S[a], ~m, N_TEST, 2)     # 홀드아웃 30% 타일에서
else:        te = sub2(S[b], 전체, N_TEST, 2)    # 지역 전체에서
```

`hon→hon`은 홍천 타일 30%에서, `dae→hon`은 홍천 전체에서 평가.
두 평가셋의 클래스 비율이 다르면 분모가 달라져 탄소 오차도 달라짐.
→ **대각 −0.63의 일부는 모델 효과가 아니라 평가셋 구성 차이일 수 있음.**

**행·열 효과는 이 문제 없음.** 비대각 6셀은 열 안에서 같은 평가셋을 씀.

| 결론 | 상태 |
|---|---|
| 행효과, 열효과 | 깨끗함. 그대로 주장 가능 |
| 대각효과 | 교락 있음. 검증 필요 |

**검증**: 비대각 평가셋을 대각과 같은 홀드아웃 타일로 바꿔 6조합 재실행(15분).
기존 결과는 두고 별도 태그(`_ho`)로 저장. 그러면 9셀이 동일 평가셋 위에 놓임.

---

## 7. 셀 코드

셀 [6]이 정의한 `PRED`, `C`, `carbon_err`, `REGIONS` 사용.
런타임이 새로 시작됐으면 §8의 복구 블록을 셀 [7] 위에 붙일 것.

### 셀 [7] 오차 분해

```python
# ============================================================
# [7] 오차 분해 — 어느 혼동이 탄소 오차를 만드는가
# ============================================================
from sklearn.metrics import confusion_matrix
NAME = {1: '침엽', 2: '활엽', 3: '혼효'}
CFG  = 'light'

def load_pred(a, b, cfg=CFG):
    z = np.load(f'{PRED}/{a[:3]}_{b[:3]}_{cfg}.npz')
    return {k: z[k] for k in z.files}

def have(a, b, cfg=CFG):
    return os.path.exists(f'{PRED}/{a[:3]}_{b[:3]}_{cfg}.npz')

def decompose(y, p):
    cm  = confusion_matrix(y, p, labels=[1, 2, 3])
    tot = sum((y == v).sum() * C[v] for v in (1, 2, 3))
    rows = [(f'{NAME[i]}→{NAME[j]}', int(cm[ai, bi]), C[j] - C[i],
             int(cm[ai, bi]) * (C[j] - C[i]) / tot * 100)
            for ai, i in enumerate([1, 2, 3])
            for bi, j in enumerate([1, 2, 3]) if i != j]
    return sorted(rows, key=lambda r: -abs(r[3]))

for a in REGIONS:
    for b in REGIONS:
        if not have(a, b): continue
        d = load_pred(a, b)
        rows  = decompose(d['y_true'], d['y_pred'])
        net   = sum(r[3] for r in rows)
        gross = sum(abs(r[3]) for r in rows)
        print(f'\n=== {a[:3]}→{b[:3]}  순 {net:+.2f}%  총 {gross:.2f}%  '
              f'상쇄율 {(1-abs(net)/gross)*100:.0f}% ===')
        for nm, n, g, c in rows[:4]:
            print(f'  {nm}  n={n:6d}  Δ계수 {g:+5.1f}  기여 {c:+6.2f}%')
```

### 셀 [7-b] 2요인 분해 — [7] 끝에 이어 붙임

```python
# ---- 2요인 분해: 행(학습) + 열(평가) + 대각 ----
M = np.full((3, 3), np.nan)
for i, a in enumerate(REGIONS):
    for j, b in enumerate(REGIONS):
        if have(a, b):
            d = load_pred(a, b)
            M[i, j] = carbon_err(d['y_true'], d['y_pred'])

if not np.isnan(M).any():
    r, c, g = M.mean(1), M.mean(0), M.mean()
    print('\n' + '=' * 56)
    print(f'{"학습＼평가":12s}' + ''.join(f'{b[:3]:>9s}' for b in REGIONS) + f'{"행평균":>10s}')
    for i, a in enumerate(REGIONS):
        print(f'{a[:3]:12s}' + ''.join(f'{M[i,j]:+9.2f}' for j in range(3)) + f'{r[i]:+10.2f}')
    print(f'{"열평균":12s}' + ''.join(f'{c[j]:+9.2f}' for j in range(3)) + f'{g:+10.2f}  ← 전체')

    F = r[:, None] + c[None, :] - g
    R = M - F
    print(f'\n잔차   최대 |{np.abs(R).max():.2f}|')
    for i, a in enumerate(REGIONS):
        print(f'{a[:3]:12s}' + ''.join(f'{R[i,j]:+9.2f}' for j in range(3)))
    dia = np.diag(R); off = R[~np.eye(3, dtype=bool)]
    print(f'대각 평균 {dia.mean():+.2f}   비대각 평균 {off.mean():+.2f}   '
          f'분리 {"O" if dia.max() < off.min() else "X"}   (우연확률 1/84)')

    # 검정 가능한 제약: 두 순환의 합 (비대각만 사용)
    c1 = M[0,1] + M[1,2] + M[2,0]
    c2 = M[0,2] + M[2,1] + M[1,0]
    print(f'\n순환제약  {c1:+.2f} vs {c2:+.2f}   차이 {abs(c1-c2):.2f}  '
          f'(노이즈 0.11)  {"통과" if abs(c1-c2) < 0.11 else "실패"}')

    # 대각 외삽 (비대각 6셀만으로 적합)
    idx = [(i, j) for i in range(3) for j in range(3) if i != j]
    A = np.array([[1] + [1 if k==i else 0 for k in range(3)]
                      + [1 if k==j else 0 for k in range(3)] for i, j in idx])
    sol = np.linalg.lstsq(A, np.array([M[i,j] for i, j in idx]), rcond=None)[0]
    print('\n지역 내 이득 (비대각 외삽 기준)')
    for i, a in enumerate(REGIONS):
        row = np.array([1] + [1 if k==i else 0 for k in range(3)]
                           + [1 if k==i else 0 for k in range(3)])
        print(f'  {a[:3]}  외삽 {row@sol:+.2f}  실측 {M[i,i]:+.2f}  이득 {M[i,i]-row@sol:+.2f}')
    print('  ※ 대각은 평가셋이 다름(홀드아웃 30% vs 전체) → 교락 있음')
```

### 셀 [7-c] 클래스별 예측 편향 — sun 행 가설 판정

```python
# ---- 예측 편향: (예측 비율 − 실제 비율) ----
#  혼동행렬 기여도는 계수가 곱해져 원인·결과가 섞임.
#  이건 모델이 어느 클래스를 과잉 예측하는지 날것으로 보여줌.
print(f'\n{"조합":12s}{"침엽":>9s}{"활엽":>9s}{"혼효":>9s}   ← 예측 − 실제 (%p)')
for a in REGIONS:
    for b in REGIONS:
        if not have(a, b): continue
        d = load_pred(a, b)
        y, p = d['y_true'], d['y_pred']
        bias = [((p == v).mean() - (y == v).mean()) * 100 for v in (1, 2, 3)]
        print(f'{a[:3]}→{b[:3]:8s}' + ''.join(f'{v:+9.2f}' for v in bias))
```

> **판정**: sun 행 3개 전부에서 활엽 양수 / 침엽 음수면 §5 가설 확정.

### 셀 [8] 타일별 분포

```python
# ============================================================
# [8] 타일별 오차 — LCA는 사업지 단위로 쓰이므로 총량보다 이쪽이 실사용 지표
# ============================================================
def coarse(t, k):
    return (t // 10000 // k) * 10000 + (t % 10000 // k)

def tile_err(d, k=1, min_n=300):
    t = coarse(d['tile'], k) if k > 1 else d['tile']
    e = [carbon_err(d['y_true'][m], d['y_pred'][m])
         for tt in np.unique(t) if (m := (t == tt)).sum() >= min_n]
    return np.array(e)

for k, km in [(1, 5), (2, 10), (4, 20)]:
    print(f'\n--- 타일 {km}km ---')
    print(f'{"조합":12s}{"타일수":>7s}{"평균":>8s}{"표준편차":>9s}'
          f'{"최소":>8s}{"최대":>8s}{"|e|>3%":>9s}')
    for a in REGIONS:
        for b in REGIONS:
            if not have(a, b): continue
            e = tile_err(load_pred(a, b), k)
            if e.size == 0: continue
            print(f'{a[:3]}→{b[:3]:8s}{e.size:7d}{e.mean():+8.2f}{e.std():9.2f}'
                  f'{e.min():+8.2f}{e.max():+8.2f}{(abs(e)>3).mean()*100:8.1f}%')
```

기준선: 대전 지역 내 표준편차 1.09% (PROJECT.md 6-6).
`|e|>3%` 열 = "남의 지역 지도를 쓰면 사업지 몇 %가 위험한가".

### 셀 [9] 계수 감도

```python
# ============================================================
# [9] 계수 감도 — 현재 결과는 계수차 10(55/65) 기준의 하한선
#   실제 목재기본밀도는 침엽 0.35~0.51 < 활엽 0.36~0.72 로 격차가 더 큼
# ============================================================
GAPS = (0, 5, 10, 15, 20, 25, 30)
print(f'{"조합":12s}' + ''.join(f'{g:>8d}' for g in GAPS) + '   ← 침엽-활엽 계수차')
for a in REGIONS:
    for b in REGIONS:
        if not have(a, b): continue
        d = load_pred(a, b)
        row = []
        for g in GAPS:
            cc = {1: 55.0, 2: 55.0 + g, 3: 55.0 + g/2}
            t = sum((d['y_true'] == v).sum() * cc[v] for v in (1,2,3))
            p = sum((d['y_pred'] == v).sum() * cc[v] for v in (1,2,3))
            row.append((p - t) * 100 / t)
        print(f'{a[:3]}→{b[:3]:8s}' + ''.join(f'{v:+8.2f}' for v in row))
```

### 셀 [10] 낙엽송 기여도

```python
# ============================================================
# [10] 낙엽송 기여도 — 5-2 가설이 탄소 오차까지 설명하는가
# ============================================================
LARCH = 13
for a in REGIONS:
    for b in REGIONS:
        if not have(a, b): continue
        d = load_pred(a, b)
        y, p, sp_ = d['y_true'], d['y_pred'], d['sp']
        con = y == 1
        if (con & (sp_ == LARCH)).sum() < 500: continue
        tot = sum((y == v).sum() * C[v] for v in (1, 2, 3))
        print(f'\n=== {a[:3]}→{b[:3]} ===')
        contrib = {}
        for nm, m in [('낙엽송', con & (sp_ == LARCH)),
                      ('기타침엽', con & (sp_ != LARCH))]:
            n = m.sum()
            contrib[nm] = sum((C[v] - C[1]) * (p[m] == v).sum() for v in (2,3)) / tot * 100
            print(f'  {nm:6s} n={n:7d}  오분류 {(p[m]!=1).mean()*100:5.1f}%  '
                  f'→활엽 {(p[m]==2).mean()*100:5.1f}%  →혼효 {(p[m]==3).mean()*100:5.1f}%  '
                  f'탄소기여 {contrib[nm]:+6.2f}%')
        s = sum(abs(v) for v in contrib.values())
        if s: print(f'  침엽 탄소오차 중 낙엽송 비중 {abs(contrib["낙엽송"])/s*100:.0f}% '
                    f'(면적 비중 {(con&(sp_==LARCH)).sum()/con.sum()*100:.0f}%)')
```

> **판정**: 면적 비중(33%)보다 탄소기여 비중이 크면 가설이 관통한 것.

---

## 8. 런타임 재시작 복구

셀 [7]~[10]은 `S`가 **필요 없음.** 셀 [2] 다시 돌릴 이유 없음.

```python
import numpy as np, os, json
from sklearn.metrics import confusion_matrix, f1_score
from google.colab import drive; drive.mount('/content/drive')

OUT  = '/content/drive/MyDrive/forest/outputs'
PRED = f'{OUT}/preds'
REGIONS = ['daejeon', 'hongcheon', 'suncheon']
C = {1: 55.0, 2: 65.0, 3: 60.0}

def carbon_err(y_true, y_pred):
    t = sum((y_true == v).sum() * C[v] for v in (1, 2, 3))
    p = sum((y_pred == v).sum() * C[v] for v in (1, 2, 3))
    return (p - t) * 100 / t

print(sorted(os.listdir(PRED)))   # 어디까지 저장됐는지
```

**셀 [6] 도중 끊긴 경우**는 `S`가 필요하므로 [1] → [2] → [6].
셀 [6]에 건너뛰기가 있어 남은 조합만 돌아감.

---

## 9. full 결과가 나오면

### 사전 예측 (지금 적어둘 것)
PROJECT.md 6-1에서 사전 예측 적중이 논거로 쓰이므로 이번에도 남길 것.

> **`sun_hon_full`이 0.46 이하면** §3-2의 "0.449 vs 0.481은 하이퍼파라미터 차이" 지지.
> 동시에 light가 full보다 높다는 뜻 — **가장 어려운 전이에서 규제 강한 모델이 낫다**는 결과.
>
> **0.47 이상이면** 재검토. [4]의 0.449가 그 조합에서만 나쁜 샘플을 뽑은 것이 되고,
> [4] 표 전체를 의심해야 함.

`dae_dae_full 0.620` vs `[4] 0.618`로 파이프라인이 [4]를 재현하는 것으로 보이나,
근거가 한 점뿐이므로 0.449 정확히를 걸지 말 것.

### 확인할 것
`dae→dae`는 light−full이 +0.004인데 `sun→hon`이 −0.035라면
**light/full 격차가 조합 난이도에 의존**한다는 뜻.
ΔF1을 조합 난이도(macro) 순으로 정렬해서 볼 것.

그러면 "3개 상승 3개 하락, 판단 보류"가
**"지역 내는 무차별, 어려운 전이일수록 light 유리"**로 바뀜.

---

## 10. 다음 할 일

### 1순위 — 셀 [7]~[10] 실행
특히 [7-c]의 예측 편향으로 §5 sun 행 가설 판정.

### 2순위 — 대각 교락 해소 (15분)
비대각 평가셋을 홀드아웃 타일로 바꿔 6조합 재실행, `_ho` 태그로 저장.
§6의 대각 효과가 실재하는지 확정.

### 3순위 — 낙엽송 노출도 실험 (30분, 미실시)
현재 "노출도로 오차 방향이 예측된다"는 **사후 설명**. 인과 근거 약함.
홍천 학습셋에서 `sp==13` 비율을 0/10/20/33%로 조절해 순천에 적용.
단조 관계가 나오면 **상관에서 인과 쪽으로** 한 발.

### 4순위 — 순천 분광 분산 확인
순천 침엽수 수종별 낙엽기 NDVI 분산 vs 홍천. §5 가설 검증.

### 5순위 — 실제 탄소계수 (박유경)
- 국립산림과학원 연구보고 14-08: 목재기본밀도, 바이오매스확장계수, 뿌리함량비
- NFI: 임상별 임목축적(m³/ha)
- **혼효림 계수 산정 방법 결정이 가장 어려움** (임의로 정하면 심사에서 지적)
- 값 나오면 `C` 한 줄만 바꾸면 전 결과 갱신

### 6순위 — 2020+ 기준 세 지역 비교 (부록용)
`year` 조건만 바꿔 재실행. 급하지 않음.

### 7순위 — U-Net
혼효림 병목 원인 규명됨(픽셀 단위로 판별 불가, 임분 단위 성질).
GPU 필요. **Colab Pro가 아니라 Pro+**여야 백그라운드 실행됨.
지금 작업(CPU)에는 결제 불필요.

---

## 11. 논문 방향

### 결론 문장
> 위성영상 기반 산림 유형 분류는 학습 지역 내에서는 LCA에 쓸 만하지만,
> 다른 지역에 적용하면 수종 구성 차이에 따라 **예측 가능한 방향으로**
> 탄소를 과대·과소추정한다.

### 방법론적 기여 — 약한 버전 금지
"분류 지표가 목적 지표를 보장하지 않는다"는 **이미 알려진 이야기**라 약함.
(원격탐사 area estimation 문헌이 오래 다룸)

대신:

> 분류 오차가 목적 지표에서 상쇄되려면 오차가 **무작위적이고 인접 클래스 간**이어야 한다.
> 학습 데이터에 없던 하위 범주가 평가 지역에 존재하면 오차가 계통적 방향을 갖고
> 상쇄가 깨진다. 그리고 이 경우 **오차의 방향을 사전에 예측할 수 있다.**

근거: 셀 [7] 상쇄율 + §4의 2요인 구조 + §4-3의 순환 제약 통과.

### 왜 위성인가 (예상 반박 대비)
임상도가 이미 있는데 왜 위성으로 다시 만드나 →
**임상도는 갱신 주기가 김.** 대전 라벨의 61%가 2024 이전이고,
그걸 버렸더니 성능이 0.607 → 0.628로 **올랐음**(오래된 라벨이 틀렸다는 직접 증거).
위성은 매년 전국을 똑같이 봄. **대체가 아니라 갱신 사이를 메우는 것.**

### 예상 질문
| 질문 | 답 |
|---|---|
| 2~4%면 괜찮지 않나 | 총량 기준이라 상쇄된 값. 사업지 단위 분산은 [8]에서. 임시 계수라 하한선 |
| 낙엽송 빼면 되지 않나 | 라벨이 임상도 기준이라 못 뺌. 낙엽송 있는 지역을 학습에 **포함**하는 게 해법 |
| 임상도 있는데 왜 위성 | 위 참조 |
| 0.62는 낮지 않나 | 혼효림이 평균을 끌어내림. 침엽·활엽은 0.84~0.86. 목표는 정확도가 아니라 오차 구조 |
| 데이터 더 넣으면 | 2지역 학습이 홍천에서 오히려 악화(0.556→0.542). **양이 아니라 다양성의 종류** |
| 난수 아니냐 | §3-2 재현 실험. 노이즈 macro 0.005, 탄소 0.11%p |
| 낙엽송이 낙엽성인 건 상식 아닌가 | 맞음. 기여는 그게 아니라 **전이를 얼마나 무너뜨리는지 정량화**한 것 |

### 갱신년도 2024+ 를 쓴 이유 (정확한 서술)
"대전에 맞춰서"가 **아님**. 02 노트북 셀 [12]에서 2020+ 와 2024+ 의
세 지역 픽셀 수를 비교한 뒤 결정. 지역마다 다른 기준을 쓰면
"지역 차이"와 "라벨 품질 차이"가 섞이기 때문.
성능 근거는 별도(대전 0.607 → 0.628, 데이터 61% 감소했는데 상승).

단, **2020+ 로 세 지역 성능 비교는 아직 안 함** → 6순위.

---

## 12. 함정 (PROJECT.md 7절 외 추가분)

| 항목 | 내용 |
|---|---|
| `rng_g` | 셀 [4]/[5]의 모듈 레벨 난수. 실행 순서 의존. 셀 [6]에서 제거됨 |
| 셀 번호 | 주석 번호(1부터)와 인덱스(0부터)가 다름. **주석 번호로 소통** |
| `tile` | 500px 고정. 크게 묶기만 가능 |
| 낙엽송 코드 | `sp == 13` |
| 대각/비대각 평가셋 | **서로 다름.** §6 참조 |
| `class_weight` | `balanced`라 단순 사전확률 편중은 부분적으로 보정됨 |
| R² | 9셀 5모수. 자유도 4. 설명력 근거로 쓰지 말 것 |
| Colab | 무료 티어는 백그라운드 실행 불가. 탭 유지 필요. Pro도 안 됨(Pro+부터) |
