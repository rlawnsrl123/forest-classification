# forest_claude1 — 대화 기록

**세션:** 2026-09-09 | 시작 문서 `HANDOFF_v3.md` → 종료 문서 `HANDOFF_v7.md` + `PAPER_DRAFT.md`
**성격:** 실험 마무리 → 계수 확정 → 프레이밍 재정립 → 집필 준비

---

## 0. 이 세션에서 일어난 일 한 문단

셀 `[19]`·`[20]`·`[21]`·`[P2]`를 새로 만들어 돌렸고, 탄소계수를 국가 검증값으로
확정했다. 그 과정에서 **v3~v6에 남아 있던 잘못된 진술 11건을 정정**했다.
가장 큰 변화는 숫자가 아니라 **프레이밍**이다. "LCA 활용 가능성"에서
"임상도 노후화 → 타 지역 모델 적용 → 전이성 평가"로 옮겼고, LCA 실무 기준
추적을 중단했다. 실험은 종료됐고 남은 것은 팀 회의 한 번과 집필이다.

---

## 1. 시간순 흐름

### 1-1. 전국 일반화 질문에서 시작
**질문:** 혼효림·낙엽송 포함 지역 몇 개로 학습하면 전국 일반화 되는 거 아닌가?

**답:** 반은 맞고 반은 이미 반증됨.
- 혼효림은 **세 지역 전부 학습에 포함**인데도 F1 0.22~0.34 → 노출도와 무관한 실패
- 낙엽송은 노출도 올리면 개선되나 **로그 선형·임계값 없음**
- "지역/수종 늘리기"는 [12](0.014%p)와 4-12(홍천 악화)로 두 번 기각됨
- 단 "전국 수십 개 지역이면 수렴하는가"는 **실험한 적 없음** → 한계로 명시

### 1-2. 셀 [9] 재검토 → [19] 신설
`[9]` 출력을 다시 읽으니 **이전 세션의 진단 자체가 틀렸다.** "부호는 계수와
무관"이 틀린 이유는 "양수만 스캔해서"가 아니라 **출력 오독**이었다. 양수
스캔에서도 이미 5/9가 부호 반전이었다.

음수 구간 재실행은 **철회**했다. g<0을 범위에 넣으면 g=0을 가로질러 전 조합이
자동으로 실패 판정되므로 정보가 없다.

대신 `[19]`(닫힌 형태)를 만들었다:
```
e(%) = (b·C)/(f·C) = [(b2−b1)·(g/2) + b3·δ] / (f·C)     (Σb = 0)
```
Σb=0이므로 계수의 절대 수준은 소거되고 g, δ만 남는다.

### 1-3. 탄소계수 확보 — 결론이 강해짐
웹 검색으로 국가 검증 계수(GIR 공표)를 찾았다.

```python
C = {1: 79.25, 2: 101.02, 3: 92.01}   # 강원 NFI 2013
# g = +21.77  (임시값 10의 2.2배)   δ = +1.87
```

**활엽 > 침엽.** v3의 "거의 같아진다"는 틀렸다. 전환계수 비 1.77이 축적 비
1.39를 이긴다.

**혼효림 중간값은 국가 인벤토리 규약(50:50)**임이 확인되어 "인위적 설정"
우려가 해소됐다.

`sun→hon` +4.21% → **+5.92%**. 결론이 뒤집히지 않고 강해졌다.

### 1-4. 신뢰 문제 제기
**사용자:** "표나 자료 수치를 너 혼자 번복하니 신뢰도 하락. 이 문제 어떻게 해결하지"

원인을 진단했다. **코드가 검증하는 것과 산문으로 쓰는 것이 분리돼 있어서**
모순이 며칠씩 살아남았다. §4-1은 "노이즈가 g에 비례"라 하고 §4-13은 노이즈를
0.11로 고정한 채 g를 움직였는데, 서로 대조된 적이 없었다.

**채택한 규칙:**
- 표에 들어가는 숫자는 **셀 출력에서만** 나온다
- [확정]/[유도]/[추정] 표시
- 주장할 때 **검산 가능한 예측**을 붙이고 다음 셀이 판정하게 한다
- 이전 진술과 충돌하면 **충돌을 먼저 지적**한다
- 범위가 있는 결과는 범위를 병기한다
- **코드는 조각 패치가 아니라 셀 전체로 제공한다**

### 1-5. [20] 실행 → 교차 검증 통과
`[20]`(닫힌 형태)과 `[7]`(오분류 분해)이 9조합 **전부 일치**. 독립 두 경로가
같은 값을 내므로 4-4 표가 확정됐다.

같은 실행에서 **가법모델이 기각**됐다. 잔차의 대각 3개가 전부 음수(−0.599),
비대각 6개가 전부 양수(+0.300), 격차가 노이즈의 3.8배. 구 계수에서도 같은
패턴(5.7배)이라 계수와 무관한 데이터의 성질이다.

v3~v5가 이걸 놓친 이유는 **순환 검정만 봤기 때문**이다. 순환은 비대각만 쓰는데
비대각 잔차가 비슷해서 상쇄된다. 자유도 1이라 원리적으로 못 본다. 잔차 행렬이
바로 위에 출력되고 있었는데 읽지 않았다.

### 1-6. 상쇄율 지표 강등
**사용자:** "상쇄율 차이가 너무 커서 의미 있는 값으로 보이지 않는데"

검증 결과 지적이 맞았다. 상쇄율은 비율이라 분자·분모 오차가 함께 실려 ±4~5%p.
인접 쌍(`dae→dae` 77.6±5.1 vs `hon→dae` 73.2±4.2)이 구별되지 않는다.

| 지표 | 지역 내 | 지역 간 | 노이즈 대비 간극 |
|---|---|---|---|
| 총오차 | 4.55~4.78 | 5.53~7.02 | **3.1배** |
| 순오차 | 0.12~1.05 | 1.51~5.92 | **1.9배** |
| 상쇄율 | 77.6~97.5 | 15.7~73.2 | 0.9배 ← 약함 |

**헤드라인을 원값 두 개로 교체:** 총오차 1.25배 vs 순오차 3.5~4.6배.
"상쇄율 평균 86% vs 49%"는 논문에서 뺀다.

16~73% 산포는 2요인으로 정렬하면 값이 된다 — 학습 순천(29.6%)과 평가
홍천(33.4%)이 각각 최악이고 최악 셀이 교집합.

### 1-7. LCA 추적 — 시작했다가 중단
실무 허용 오차를 찾다가 5%가 반복 등장하는 것을 확인했고(산림탄소상쇄 운영표준
이차적 배출량 기본값, LCA 실무 1차 데이터 편차), 이어서 생물기원 탄소 산정
방식이 **0/0, −1/+1 등 최소 4가지**이고 표준 간 권고가 갈린다는 것을 확인했다.

더 근본적으로: **산림 탄소축적량 → LCA 경로가 표준화돼 있지 않다.** LCA에
들어가는 건 목재제품의 생물기원 탄소이고, 그건 목재 질량으로 계산하지 임상
지도로 계산하지 않는다.

**사용자:** "결과가 부정이니 어렵네. 지금까지 뭘 한 거지?"

→ 이 세션의 전환점. **계산에 LCA가 한 번도 들어가지 않았다**는 것을 확인했다.
쓴 계수는 전부 국가 온실가스 인벤토리 체계(GIR 검증 계수, NFI 축적, 50:50 규약,
IPCC CF 0.5)의 값이다. LCA 산정 방식이 무엇이든 계산은 안 바뀐다.

**결론: LCA는 서론 배경 문장으로만 두고, 논문이 LCA 질문에 답한다고 주장하지
않는다.** 실무 기준 추적은 중단.

### 1-8. 명분 정정 — 두 번 틀린 뒤 제자리
| 시점 | 진술 | 판정 |
|---|---|---|
| v3~v5 | "라벨 **없는** 지역이 대상" | 틀림. 임상도는 전국에 있음 |
| v6 | "명분은 파운데이션 모델뿐" | 과잉 교정 |
| **v7** | **"임상도 노후화"** | **사용자가 원래 말한 것** |

**사용자:** "이거는 애초에 주제잖아. 임상도가 오래된 경우 위성을 통해
접근해보자 여서 지역 전이 등을 알아봤던 거잖아"

맞다. 임상도는 5년 주기 갱신이고 갱신 시점이 지역마다 다르다. 갱신이 지연된
지역의 지도를 만들려면 **그 지역의 현재 라벨이 없으므로** 라벨이 최신인 다른
지역으로 학습해야 한다. 학습 지역 ≠ 적용 지역. 이게 공간 전이다.

### 1-9. 서론 근거 확보 — 02-[12]
노트북에서 실제 숫자를 찾았다.

| 지역 | 전체 산림 px | 2020+ | 2024+ | 2024+ 잘림 |
|---|---|---|---|---|
| 대전 | 2,605,226 | 50.9% | 39.5% | 60.5% |
| 홍천 | 14,976,948 | 45.4% | 31.6% | 68.4% |
| 순천 | 5,640,120 | **92.0%** | 33.1% | 66.9% |
| 합계 | 23,222,294 | 57.3% | 32.9% | **67.1%** |

**순천이 결정적.** 2020+ 기준 92%가 남는데 2024+ 기준 33%. 순천 임상도의
58.9%가 2020~2023에 집중 갱신됐고 대전·홍천은 11~14%뿐이다.

→ "임상도가 오래됐다"가 아니라 **"얼마나 오래됐는지가 지역마다 다르다"**.

기존 핸드오프의 "61%"는 **대전 기준**(60.5%)이었다. 전체는 67.1%.

### 1-10. [P] 교란 발견 → [P2]
서론 근거로 쓰려던 `0.607 → 0.628`을 검증했더니 **학습·평가를 모두 2024+로
바꾼 실험**이었다. 평가셋 구성이 함께 변해(활엽 50.4→45.7%) 교란돼 있었고,
클래스별로는 활엽 −0.020, accuracy −0.011이었다.

**추가 정정:** "학습 데이터 61% 감소했는데 성능 상승"도 틀렸다. 두 실험 모두
`min(200_000, ...)`로 학습 픽셀 200,000개 동일. 줄어든 건 학습 가능 *풀*이다.

`[P2]`를 만들어 **평가셋을 2024+로 고정**하고 학습셋만 A/B/C로 바꿨다.

| 학습셋 | 침엽 | 활엽 | **혼효** | macro | acc |
|---|---|---|---|---|---|
| A 전체 라벨 | 0.765 | 0.793 | 0.299 | 0.619 | 0.706 |
| B 2024+ 만 | 0.762 | 0.784 | **0.342** | 0.629 | 0.698 |
| C 2024 미만 | 0.765 | 0.793 | **0.245** | 0.601 | 0.707 |

**라벨 신선도 효과는 혼효림에만 있다.** 침엽·활엽은 재현 변동 범위이거나 오히려
B가 낮고, accuracy도 B가 최저. 혼효림만 0.245→0.342 (노이즈의 19배).

해석: 혼효림은 침엽·활엽 혼합 비율(25~75%)로 정의되어 천이·간벌·조림으로 가장
빨리 변한다. 순림은 5년 사이 임상이 바뀌는 일이 드물다.

### 1-11. 3지역 표본 문제 — 마지막 쟁점
**사용자:** "a b c 지역만 했는데 이걸로 뭔가 낼 수 있다는 게 이해가 안 됨.
d로 학습해서 더 나은 결과가 나온다면? 홍천 데이터도 오히려 단점 아닌가?"

**핵심 구분:**

| 주장 유형 | D·E·F 추가 시 | 예 |
|---|---|---|
| **A "얼마나 나쁜가"** | **바뀜** | 순오차 5.92%, 6조합 중 5개 통과 |
| **B "이런 일이 일어나는가"** | **안 바뀜** | 상쇄가 깨지는 경우가 있다 |

논문은 **B만 주장**하고 A는 "저희 세 지역에서는 이랬습니다"로만 쓴다.

**"D 학습이 더 나으면?"** — 그건 논문에 이롭다. 지금도 조합마다 순오차가
1.51~5.92%로 4배 갈린다. D가 더 나으면 "학습 지역 선택이 결과를 좌우한다"는
주장이 강해진다. 논문이 무너지는 경우는 "학습 지역을 뭘 쓰든 결과가 같다"가
나올 때뿐인데, 이미 4배 차이가 관측됐다.

**"홍천 데이터가 왜 강점인가"** — 얻은 데이터가 아니라 **조작한 실험**이라서다.

| 고정 | 바꾼 것 |
|---|---|
| 지역(홍천), 기후, 지형, 위도, 라벨 갱신 시점, 학습 픽셀 수, 클래스 구성, 평가셋 | **학습셋 낙엽송 비율 0→33% (8단계)** |

전이 9칸은 **관찰**이라 대전·순천이 수종·기후·도시화·라벨 시점이 전부 달라
원인을 못 가린다. 홍천 실험은 **한 지역 안에서 변수 하나만** 바꿨다. 여기엔
지역이 하나만 등장하므로 "지역 표본 3개"라는 말이 적용될 자리가 없다.

관찰은 표본 수가 중요하고, 실험은 **통제**가 중요하다.

단 한계는 크다 — 낙엽송 한 종, 홍천 한 지역, 오차의 1/4만 설명.
그래서 주장은 **"이 형태의 인과가 존재한다"**까지이고 **"모든 수종·지역에서
기울기 −0.203"**은 아니다.

---

## 2. 정정된 진술 전체 목록

| # | 진술 | 정정 |
|---|---|---|
| 1 | "부호는 계수와 무관" (이전 세션) | 기각. 5/9가 부호 반전 |
| 2 | "원인은 [9]가 양수만 스캔" | 진단도 틀림. **출력 오독** |
| 3 | "[9] 음수 재실행 필요" | **철회.** g=0 가로질러 자동 전부 실패 |
| 4 | "계수차 2~3이면 결론 뒤집힘" | 해소. 실제 g = 21.77 |
| 5 | "혼효 60은 인위적" | 국가 인벤토리 규약(50:50) |
| 6 | "진폭 = 0.198·b3 정확히 맞음" | **근사임** (0.203~0.215) |
| 7 | "검출 하한 g≥0.9" | **폐기.** 노이즈 g-비례와 모순 |
| 8 | "가법모델 성립" | **기각.** 잔차 대각/비대각 구조 |
| 9 | "총오차 일정" | **1.25배 증가** |
| 10 | "sun→hon 38.3배" | 손계산 오류. 24.7배 |
| 11 | "상쇄율 86% vs 49%" | **강등.** 인접 쌍 오차 막대 겹침 |
| 12 | "실무자가 F1으로 판단" | 근거 없음. 실무 관행 미조사 |
| 13 | "라벨 없는 지역이 대상" | **노후화**가 맞음 |
| 14 | "명분은 파운데이션 모델뿐" | 과잉 교정. 노후화가 원래 명분 |
| 15 | "학습 데이터 61% 감소했는데 성능 상승" | 학습량 동일(200,000). 상승도 교란 |
| 16 | "61%" | 대전 기준. 전체는 **67.1%** |
| 17 | `[P]` 0.607→0.628 | **평가셋 교란.** [P2]로 재실험 |
| 18 | 3% 문턱 | **근거 없음.** 분포로 보고 |
| 19 | 시군구 계수 확보 필요 | 불필요. 전국 계수로도 8/9 부호 동일 |

**코드 버그 4건:** `[20]` KeyError(3글자 키), `[20]` MIN_PX=5000(전 타일 탈락),
`[7]` 구 계수로 실행(`[R]`의 C 미교체), 패치의 `G` 미정의.

---

## 3. 새로 만든 셀

| 셀 | 내용 | 상태 |
|---|---|---|
| `[19]` | 계수 감도 닫힌 형태. `[9]` 격자 스캔 대체 | 완료 |
| `[20]` | 확정 계수 적용, 9조합 + δ 감도 + 사업지 | 완료 |
| `[7]` 수정 | `G` 행렬 추가, 변수 충돌 정리, 잔차 판정 | 완료 |
| `[21]` | 문턱별 재집계 + 타일 오차 분포 | 미실행 (선택) |
| `[P2]` (01) | 라벨 시의성, 평가셋 고정판 | 완료 |

**계수 프리앰블 (모든 셀 앞에 필수):**
```python
C = {1: 79.25, 2: 101.02, 3: 92.01}
NOISE = 0.11 * (C[2] - C[1]) / 10.0      # → 0.239
```
`[R]`의 C는 구 임시값 55/65/60이며 `[20]` 실행만으로는 안 바뀐다.

---

## 4. 최종 프레이밍

### 제목
> 인공위성 영상 기반 산림 유형 분류 AI 모델 개발 및 **지역 간 전이성 평가**

### 연구 질문
> 라벨이 최신인 지역으로 학습한 모델이 다른 지역에도 통용되는가?
> 통용된다면 어떤 조건에서인가?

### 논지
1. 임상도 갱신이 지역마다 다르다(67.1%가 2024년 이전, 2020+ 잔존율 45~92%)
2. 라벨 신선도는 **혼효림에만** 영향(0.245→0.342)
3. 전이 시 **총오차 1.25배, 순오차 3.5~4.6배** — 상쇄 붕괴
4. 가법모델 잔차의 대각/비대각 구조로 나타남(노이즈의 3.8~5.7배)
5. **분류 정확도로 예측 불가** (F1 차 0.002, 순오차 2배)
6. 산포는 학습·평가 지역 효과로 정렬. 최악 셀이 교집합
7. 원인 하나를 **단일 지역 내 통제 실험**으로 특정(로그 선형, 임계값 없음)
8. 모델 용량으로 안 줄어듦(0.33%p vs 6.7%p)
9. 계수 설정에 강건(8/9 노이즈 초과, 두 시나리오 부호 동일)

### 주장 가능 여부 — 지역 수 의존도 기준
| 주장 | 지역 수 영향 |
|---|---|
| 낙엽송 노출도가 오차를 지배한다 | **없음** (단일 지역 통제 실험) |
| 상쇄가 깨지는 경우가 실재한다 | 없음 (존재 주장) |
| F1으로 예측할 수 없다 | 없음 (반례 1건) |
| 라벨 신선도는 혼효림에만 영향 | 없음 (대전 단일) |
| "전이 시 평균 X%p 악화" | **치명적 → 주장 안 함** |
| "6조합 중 5개 통과 = 통과율" | **치명적 → 주장 안 함** |

### 논문에서 빼는 것
- LCA 산정 체계, 실무 허용 오차, "치명적" 류 표현
- 사업지 3%/5% 초과 비율 → **타일 오차 분포**로 대체
- 20km 집계(그룹 5개 내외)
- 상쇄율 평균 비교
- "매우 드물다" 류 희소성 주장(체계적 검색 미실시)

---

## 5. 남은 일

| 순 | 내용 | 시간 | 필수 |
|---|---|---|---|
| 1 | **팀 회의** — 제목·프레이밍 승인 | — | ★ |
| 2 | **집필** (`PAPER_DRAFT.md`) | — | ★ |
| 3 | `[10]`, `[18]` 새 계수 재실행 | 5분 | 권장 |
| 4 | `[5]`/`[6]` 노이즈 재측정 (0.239가 [유도]) | 10분 | 권장 |
| 5 | `[21]` 실행 → 타일 분포 그림 | 5분 | 권장 |
| 6 | `[P2]` 클래스 동수 표집 재확인 | 20분 | 선택 |
| 7 | 방향 B (완도·제주 상록활엽) | 1일 | 선택 |

**팀 회의 안건은 둘뿐이다.** 제목 변경 승인, 서론 프레이밍 승인.
LCA 실무 질의는 **철회**(표준화 자체가 안 됨).

---

## 6. 산출물

| 파일 | 내용 |
|---|---|
| `HANDOFF_v7.md` | 프로젝트 전체 상태. 다음 세션이 이것만 읽으면 됨 |
| `PAPER_DRAFT.md` | 논문 초안. 초록~결론 본문, 표 11개, 그림 후보 7개 |
| `forest_claude1.md` | 이 파일. 세션 기록 |

**PAPER_DRAFT 수정 예정 (합의됨, 미반영):**
- 4.4절을 "존재 주장 vs 빈도 주장" 구분으로 재작성
- 3.6절 앞에 "관찰과 실험의 구분" 한 단락 추가

---

## 7. 다음 세션에게

- **HANDOFF_v7.md §1을 먼저 읽을 것.** 이 프로젝트에서 반복된 실패는
  "결과보다 한 칸 위로 말하기"다
- 숫자는 **셀 출력에서만** 가져온다. 손계산을 표로 만들지 않는다
- **코드는 셀 전체로 제공한다.** 조각 패치는 이 세션에서 버그 4건을 만들었다
- macro 평균만 보지 않는다. `[P2]`가 클래스별로 방향이 갈렸다
- 검정 하나로 판정하지 않는다. 가법모델이 순환 검정만 보다 3버전 동안 틀렸다
- **없는 정당화를 만들지 않는다.** LCA 실무 기준을 찾아 붙이려다 있는 결과까지
  흔들리게 만들었다
