# 인수인계 — 지역 간 전이 탄소 오차 분석

이 파일은 이전 대화의 연속입니다. `PROJECT.md`와 함께 읽으세요.
`PROJECT.md`가 프로젝트 전체 상태, 이 파일이 **지금 하고 있는 작업**입니다.

작성 시점: 2026-09-07
노트북: `03_experiments.ipynb` (Colab, 무료 티어)

---

## 0. 지금 상태 한 줄

셀 [6]을 실행 중(예측 저장, 약 1시간). 끝나면 셀 [7]~[10]으로 분석.
아래 셀 [7]~[10] 코드는 이미 작성됨. 그대로 쓰면 됨.

---

## 1. 노트북 구조

주석 번호와 실제 셀 순서가 다름. **주석 번호로 통일해서 부름.**

| 주석 번호 | 셀 인덱스 | 내용 | 상태 |
|---|---|---|---|
| [1] | 0 | 셋업, `make_feats`, `extract_samples` | 완료 |
| [2] | 1 | `S` = 세 지역 샘플 로드 | 완료 |
| [3] | 2 | 대전 검증 | 완료 |
| [4] | 3 | 전이 실험 12조합 | 완료 (결과는 PROJECT.md 5-4) |
| [5] | 4 | 탄소 오차 6조합 | 완료, **재실행 금지** |
| [6] | 5 | 예측 저장 9조합×2설정 | **실행 중** |
| [7]~[10] | — | 분석 (아래 코드 있음) | 대기 |

### 셀 [5]를 재실행하면 안 되는 이유
`rng_g`가 모듈 레벨 난수 생성기라 셀 실행 순서에 따라 다른 샘플을 뽑음.
다시 돌리면 [4]의 결과를 재현할 수 없게 됨. 셀 [6]은 조합별 독립 시드로 이 문제를 제거함.

---

## 2. 데이터 구조 (중요)

### npz 키
`{OUT}/{name}_samples.npz` 의 키는 **`X, y, tile, sp`** 네 개.

- `X` : (n, 38) float32 — 피처
- `y` : int — 1 침엽 / 2 활엽 / 3 혼효
- `tile` : `(row//500)*10000 + (col//500)` — 5km 격자 번호
- `sp` : `KOFTR_GROU` 수종코드. **낙엽송 = 13**

**raw row/col은 저장 안 됨.** `tile`에서 역산은 가능:
- `row//500 = tile // 10000`
- `col//500 = tile % 10000`

따라서 타일을 **더 크게 묶는 건 가능**(10km, 20km), 더 잘게는 불가.

### 예측 파일
셀 [6]이 `{OUT}/preds/{a3}_{b3}_{cfg}.npz` 18개를 만듦.
- `a3`, `b3` : `dae`, `hon`, `sun`
- `cfg` : `light` (트리 80, leaf 20, sqrt) 또는 `full` (트리 150, leaf 5)
- 키: `y_true`, `y_pred`, `tile`, `sp`, `cfg`

**셀 [7]~[10]은 이 파일만 읽음. `S` 불필요, 재학습 불필요.**

---

## 3. 이번에 나온 결과 (셀 [5], 경량 설정)

| 조합 | macro | 탄소오차 | 학습지역 낙엽송 |
|---|---|---|---|
| sun → hon | 0.481 | **+4.20%** | 없음 → 33% |
| sun → dae | 0.587 | +2.05% | 없음 → 없음 |
| dae → hon | 0.565 | +2.01% | 없음 → 33% |
| hon → dae | 0.582 | −1.23% | 33% → 없음 |
| dae → sun | 0.553 | −1.29% | 없음 → 없음 |
| hon → sun | 0.544 | −2.50% | 33% → 없음 |

### 해석
지역 내 ±1%가 전이에서 1.2~4.2%로 커짐. **크기보다 부호에 구조가 있음.**

- 낙엽송을 못 본 모델이 홍천에 가면 **과대추정 (+)**
  → 낙엽송을 활엽으로 오분류 → 활엽 계수(65) > 침엽(55)
- 홍천에서 배운 모델이 상록 지역에 가면 **과소추정 (−)**
  → 침엽을 과잉 예측

노출도 차이(학습 − 평가)와 부호가 정확히 대응:
```
−33 → +4.20, +2.01     (낙엽송 없는 모델 → 홍천)
+33 → −2.50, −1.23     (홍천 모델 → 상록 지역)
  0 → +2.05, −1.29     (방향성 없음, 크기 작음)
```

---

## 4. 확정된 주의사항 (이전 대화에서 교정된 것들)

### ① 경량 vs 무거운 설정 — 아무 주장도 하지 말 것
6조합 중 3개 상승, 3개 하락. "전부 개선"은 **틀림**.

| | 무거운([4]) | 경량([5]) | |
|---|---|---|---|
| dae→hon | 0.556 | 0.565 | ↑ |
| sun→dae | 0.554 | 0.587 | ↑ |
| sun→hon | 0.449 | 0.481 | ↑ |
| dae→sun | 0.564 | 0.553 | ↓ |
| hon→dae | 0.594 | 0.582 | ↓ |
| hon→sun | 0.555 | 0.544 | ↓ |

게다가 [4]와 [5]는 **하이퍼파라미터도 다르고 샘플도 다름**(rng_g 상태 차이).
비교 자체가 성립 안 함. 셀 [6]이 같은 시드로 양쪽을 돌리므로 그때 판가름.

### ② 0.449 vs 0.481의 원인
난수가 아니라 **하이퍼파라미터 차이**. (난수 문제는 별개로 실재하지만 이 두 숫자의 원인은 아님)

### ③ "실제 계수 넣으면 6~8%"는 추정치
계수 격차로부터의 외삽. 측정값 아님. 셀 [9]가 이걸 실측함.

### ④ 갱신년도 2024+ 를 쓴 이유
"대전에 맞춰서"가 **아님**. 02 노트북 셀 [12]에서 2020+ 와 2024+ 의 세 지역
픽셀 수를 비교한 뒤 결정. 지역마다 다른 기준을 쓰면 "지역 차이"와
"라벨 품질 차이"가 섞이기 때문. 성능 근거는 별도로 있음(대전 0.607 → 0.628).

단, **2020+ 로 세 지역 성능 비교는 아직 안 함.** 부록용 과제.

---

## 5. 셀 [7]~[10] 코드

셀 [6]이 정의한 `PRED`, `C`, `carbon_err`, `REGIONS`를 씀.
런타임이 새로 시작됐으면 셀 [7] 위에 §6의 복구 블록을 붙일 것.

### 셀 [7] 오차 분해

```python
# ============================================================
# [7] 오차 분해 — 어느 혼동이 탄소 오차를 만드는가
#   지역 내에서 오차가 작았던 건 혼효↔활엽(계수차 5)이 상쇄됐기 때문.
#   전이에서 상쇄가 깨졌다면 침엽↔활엽(계수차 10)이 한 방향으로 몰린 것.
# ============================================================
from sklearn.metrics import confusion_matrix
NAME = {1: '침엽', 2: '활엽', 3: '혼효'}
CFG  = 'light'

def load_pred(a, b, cfg=CFG):
    z = np.load(f'{PRED}/{a[:3]}_{b[:3]}_{cfg}.npz')
    return {k: z[k] for k in z.files}

def decompose(y, p):
    cm  = confusion_matrix(y, p, labels=[1, 2, 3])
    tot = sum((y == v).sum() * C[v] for v in (1, 2, 3))
    rows = [(f'{NAME[i]}→{NAME[j]}', int(cm[ai, bi]), C[j] - C[i],
             int(cm[ai, bi]) * (C[j] - C[i]) / tot * 100)
            for ai, i in enumerate([1, 2, 3])
            for bi, j in enumerate([1, 2, 3]) if i != j]
    return sorted(rows, key=lambda r: -abs(r[3]))

for a in REGIONS:
    for b in REGIONS:
        d = load_pred(a, b)
        rows  = decompose(d['y_true'], d['y_pred'])
        net   = sum(r[3] for r in rows)
        gross = sum(abs(r[3]) for r in rows)
        print(f'\n=== {a[:3]}→{b[:3]}  순 {net:+.2f}%  총 {gross:.2f}%  '
              f'상쇄율 {(1-abs(net)/gross)*100:.0f}% ===')
        for nm, n, g, c in rows[:4]:
            print(f'  {nm}  n={n:6d}  Δ계수 {g:+5.1f}  기여 {c:+6.2f}%')
```

### 셀 [8] 타일별 분포

```python
# ============================================================
# [8] 타일별 오차 — LCA는 사업지 단위로 쓰이므로 총량보다 이쪽이 실사용 지표
# ============================================================
def coarse(t, k):
    return (t // 10000 // k) * 10000 + (t % 10000 // k)

def tile_err(d, k=1, min_n=300):
    t = coarse(d['tile'], k) if k > 1 else d['tile']
    e = [carbon_err(d['y_true'][m], d['y_pred'][m])
         for tt in np.unique(t) if (m := (t == tt)).sum() >= min_n]
    return np.array(e)

for k, km in [(1, 5), (2, 10), (4, 20)]:
    print(f'\n--- 타일 {km}km ---')
    print(f'{"조합":12s}{"타일수":>7s}{"평균":>8s}{"표준편차":>9s}'
          f'{"최소":>8s}{"최대":>8s}{"|e|>3%":>9s}')
    for a in REGIONS:
        for b in REGIONS:
            e = tile_err(load_pred(a, b), k)
            if e.size == 0: continue
            print(f'{a[:3]}→{b[:3]:8s}{e.size:7d}{e.mean():+8.2f}{e.std():9.2f}'
                  f'{e.min():+8.2f}{e.max():+8.2f}{(abs(e)>3).mean()*100:8.1f}%')
```

### 셀 [9] 계수 감도

```python
# ============================================================
# [9] 계수 감도 — 현재 결과는 계수차 10(55/65) 기준의 하한선
#   실제 목재기본밀도는 침엽 0.35~0.51 < 활엽 0.36~0.72 로 격차가 더 큼
# ============================================================
GAPS = (0, 5, 10, 15, 20, 25, 30)
print(f'{"조합":12s}' + ''.join(f'{g:>8d}' for g in GAPS) + '   ← 침엽-활엽 계수차')
for a in REGIONS:
    for b in REGIONS:
        d = load_pred(a, b)
        row = []
        for g in GAPS:
            cc = {1: 55.0, 2: 55.0 + g, 3: 55.0 + g/2}
            t = sum((d['y_true'] == v).sum() * cc[v] for v in (1,2,3))
            p = sum((d['y_pred'] == v).sum() * cc[v] for v in (1,2,3))
            row.append((p - t) * 100 / t)
        print(f'{a[:3]}→{b[:3]:8s}' + ''.join(f'{v:+8.2f}' for v in row))
```

### 셀 [10] 낙엽송 기여도

```python
# ============================================================
# [10] 낙엽송 기여도 — 5-2 가설이 탄소 오차까지 설명하는가
# ============================================================
LARCH = 13
for a in REGIONS:
    for b in REGIONS:
        d = load_pred(a, b)
        y, p, sp_ = d['y_true'], d['y_pred'], d['sp']
        con = y == 1
        if (con & (sp_ == LARCH)).sum() < 500: continue
        tot = sum((y == v).sum() * C[v] for v in (1, 2, 3))
        print(f'\n=== {a[:3]}→{b[:3]} ===')
        contrib = {}
        for nm, m in [('낙엽송', con & (sp_ == LARCH)),
                      ('기타침엽', con & (sp_ != LARCH))]:
            n = m.sum()
            contrib[nm] = sum((C[v] - C[1]) * (p[m] == v).sum() for v in (2,3)) / tot * 100
            print(f'  {nm:6s} n={n:7d}  오분류 {(p[m]!=1).mean()*100:5.1f}%  '
                  f'→활엽 {(p[m]==2).mean()*100:5.1f}%  →혼효 {(p[m]==3).mean()*100:5.1f}%  '
                  f'탄소기여 {contrib[nm]:+6.2f}%')
        s = sum(abs(v) for v in contrib.values())
        if s: print(f'  침엽 탄소오차 중 낙엽송 비중 {abs(contrib["낙엽송"])/s*100:.0f}% '
                    f'(면적 비중 {(con&(sp_==LARCH)).sum()/con.sum()*100:.0f}%)')
```

---

## 6. 런타임 재시작 복구

셀 [7]~[10]은 `S`가 **필요 없음**. 셀 [2](샘플 로드)를 다시 돌릴 이유 없음.
아래를 셀 [7] 위에 붙이면 단독 실행 가능.

```python
import numpy as np, os, json
from sklearn.metrics import confusion_matrix, f1_score
from google.colab import drive; drive.mount('/content/drive')

OUT  = '/content/drive/MyDrive/forest/outputs'
PRED = f'{OUT}/preds'
REGIONS = ['daejeon', 'hongcheon', 'suncheon']
C = {1: 55.0, 2: 65.0, 3: 60.0}

def carbon_err(y_true, y_pred):
    t = sum((y_true == v).sum() * C[v] for v in (1, 2, 3))
    p = sum((y_pred == v).sum() * C[v] for v in (1, 2, 3))
    return (p - t) * 100 / t

print(sorted(os.listdir(PRED)))   # 어디까지 저장됐는지
```

**셀 [6] 도중에 끊긴 경우**는 `S`가 필요하므로 [1] → [2] → [6] 순서.
셀 [6]에 이미 건너뛰기가 있어서 남은 조합만 돌아감.

---

## 7. 각 셀에서 확인할 것

### 셀 [7] — 가장 중요
**`상쇄율`**을 봄. 예상 시나리오:
- 지역 내 3개는 높고(90%대), `sun→hon`이 크게 낮음
- 그러면 "상쇄가 성립하는 조건과 깨지는 조건"이 수치로 확인됨

지역 내에서도 상쇄율이 낮게 나오면 해석을 다시 짜야 함.
`침엽→활엽` 기여도가 sun→hon에서 압도적으로 크고 양수인지 확인.

### 셀 [8]
대전 지역 내 표준편차 1.09%(PROJECT.md 6-6)가 기준선.
`|e|>3%` 열이 "남의 지역 지도를 쓰면 사업지 몇 %가 위험한가".
타일 크기에 따라 달라지는지도(스케일 의존성) 볼 것.

### 셀 [9]
계수차 20~30 열이 실제 오차의 상한선 추정.
실제 계수 나오면 `C` 한 줄만 바꿔서 전 결과 갱신.

### 셀 [10]
마지막 줄 **`낙엽송 비중 vs 면적 비중`** 비교가 핵심.
면적 33%인데 탄소기여가 그보다 크면 가설이 관통한 것.

---

## 8. 이 다음에 할 것

### 우선순위 1 — 낙엽송 노출도 실험 (미실시, 30분)
현재 "낙엽송 노출도로 오차 방향이 예측된다"는 **사후 설명**임.
6조합을 보고 나서 만든 이야기라 인과 근거가 약함.

검증 방법: 홍천 학습셋에서 `sp==13` 픽셀 비율을 0%, 10%, 20%, 33%로
조절해가며 순천에 적용. 노출도와 오차의 관계가 단조적인지 직접 확인.
`species.tif`가 세 지역 다 있으므로 데이터는 준비됨.

되면 **상관에서 인과 쪽으로** 한 발 나감.

### 우선순위 2 — 실제 탄소계수 (박유경)
- 국립산림과학원 연구보고 14-08: 목재기본밀도, 바이오매스확장계수, 뿌리함량비
- NFI: 임상별 임목축적(m³/ha)
- **혼효림 계수 산정 방법 결정이 가장 어려움** (임의로 정하면 심사에서 지적)

### 우선순위 3 — 2020+ 기준 세 지역 비교 (부록용)
`year` 조건만 바꿔 같은 코드 재실행. 급하지 않음.

### 우선순위 4 — U-Net
혼효림 병목 원인 규명됨(픽셀 단위로 판별 불가, 임분 단위 성질).
GPU 필요. Colab Pro가 아니라 **Pro+**여야 백그라운드 실행됨.
지금 작업(CPU 1시간)에는 결제 불필요.

---

## 9. 논문 방향 (합의된 것)

### 결론 문장
> 위성영상 기반 산림 유형 분류는 학습 지역 내에서는 LCA에 쓸 만하지만,
> 다른 지역에 적용하면 수종 구성 차이에 따라 예측 가능한 방향으로
> 탄소를 과대·과소추정한다.

### 방법론적 기여 (약한 버전 → 강한 버전으로 교정됨)
"분류 지표가 목적 지표를 보장하지 않는다"는 **이미 알려진 이야기**라 약함.
대신 이렇게:

> 분류 오차가 목적 지표에서 상쇄되려면 오차가 **무작위적이고 인접 클래스 간**이어야 한다.
> 학습 데이터에 없던 하위 범주가 평가 지역에 존재하면 오차가 계통적 방향을 갖고
> 상쇄가 깨진다. 그리고 이 경우 **오차 방향을 사전에 예측할 수 있다.**

셀 [7]의 상쇄율이 이 명제의 측정값. 셀 [8]의 우선순위 1 실험이 인과 근거.

### 예상 질문 대비
- **"2~4%면 괜찮지 않나"** → 총량 기준이라 상쇄된 값. 사업지 단위 분산은 셀 [8]에서. 임시 계수라 하한선.
- **"낙엽송 빼면 되지 않나"** → 라벨이 임상도 기준이라 못 뺌. 낙엽송 있는 지역을 학습에 포함하는 게 해법.
- **"임상도 있는데 왜 위성"** → 임상도는 갱신 주기가 김. 대전 라벨 61%가 2024 이전이고, 그걸 버리니 성능이 올랐음. 대체가 아니라 갱신 사이를 메우는 것.
- **"0.62는 낮지 않나"** → 혼효림이 평균을 끌어내림. 침엽·활엽은 0.84~0.86. 목표는 정확도가 아니라 오차 구조 규명.
- **"데이터 더 넣으면"** → 2지역 학습이 홍천에서 오히려 악화(0.556→0.542). 양이 아니라 다양성의 종류.

---

## 10. 알려진 함정 (PROJECT.md 7절 외 추가분)

| 항목 | 내용 |
|---|---|
| `rng_g` | 셀 [4]/[5]의 모듈 레벨 난수. 실행 순서 의존. 셀 [6]에서 제거됨 |
| 셀 번호 | 주석 번호(1부터)와 실제 인덱스(0부터)가 다름. 주석 번호로 소통 |
| `tile` | 500px 고정. 크게 묶기만 가능 |
| 낙엽송 코드 | `sp == 13` |
| Colab | 무료 티어는 백그라운드 실행 불가. 탭 유지 필요 |
